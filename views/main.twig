{% import "components/html.twig" as htmls %}
{{ htmls.html("bg-main.jpg")}}

   

{% block header %}  
{% import "components/header.twig" as headers %}
    {{headers.header()}}
{% endblock %}


{% block navbar %} 
{% import "components/navbar.twig" as navbars %} 
     {{navbars.navbar()}}
{% endblock %}


{% block main %}  
{% import "components/difCard.twig" as usersCards %}

    {{usersCards.userCard(
        allExercise
    )}}       
<script src="../public/script/login-FE.js"></script>
  <script >

  function OnLoad(){

    const checkUserId = localStorage.getItem('user-id');
    
    fetch('/main/api/seassion',{
        method: "POST",
        headers:{'Content-Type': 'application/json'},
         body: JSON.stringify({checkUserId})
    }).then(response=>
    {
          response.json().then(r=>
          {
            console.log(r);;
          })
    })

        {# window.location.href ='/login' #}
    
    }
    window.onload= OnLoad();
    </script>

{% endblock %}

